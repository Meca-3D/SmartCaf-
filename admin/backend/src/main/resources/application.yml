spring:
  application:
    name: smartcafé

  datasource:
    url: jdbc:mysql://localhost:3306/smartcaf%C3%A9?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: leo
    password: leo

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
  
server:
  port: 8081
  
cors:
  allowed-origins: http://localhost:3000,http://localhost:3001,http://localhost:5173
  
jwt:
  secret: VotreCleSecreteSuperLongueEtComplexePourLaProductionChangezCeci
  expiration: 86400000 # 24 heures en millisecondes
